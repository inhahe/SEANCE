# Build output
cpp/build/
build/
out/
*.exe
*.dll
*.lib
*.pdb
*.obj
*.ilk
*.exp
*.idb
*.tlog
*.log

# Visual Studio
.vs/
*.suo
*.user
*.opensdf
*.sdf
*.VC.db
*.VC.opendb
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# VS Code / JetBrains
.vscode/
.idea/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Third-party deps (fetched via setup_dependencies.bat — don't bloat the repo)
cpp/third_party/
cpp/src_imgui_backup/

# Plugin scan caches and per-machine config
soundshop_plugins_cache.dat
imgui.ini
SoundShop.node_editor.json

# Ad-hoc / scratch
*.tmp
*.bak
*.orig
*~
name.txt

# Release archives (built by hand, not source)
release.zip
release.rar

# Audio renders / capture scratch
*.wav
captures/
after_j_samples/
after_j.mod

# Self-test scratch output
cpp/selftest_out*/
cpp/st_*.txt
cpp/st*_err.txt

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Claude Code session/memory data — keep private
.claude/

# Unrelated project sitting in this directory (has its own .git)
cservice-znc-module/

# Local-only. We publish straight from this directory now instead of copying an
# allowlisted subset into D:\github (copy_to_seance.bat). These were also purged
# from history: private notes, local build/launch scripts, saved projects and
# the machine-specific plugin config.
CLAUDE.md
.orchestrator_history
agent-todo.md
build.bat
cpp/build.bat
release.bat
seance.bat
dumpdir.bat
copy_to_seance.bat
distribute.bat.old
*.ssp
soundshop_plugins.cfg
todo.txt
todo.disk-*.txt
